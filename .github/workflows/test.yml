name: Test SSH Connection

on:
  workflow_dispatch:

jobs:
  ssh-test:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar info inicial
        run: |
          echo "🏁 Iniciando teste SSH"
          echo "Host: ${{ secrets.SSH_HOST }}"
          echo "Usuário: ${{ secrets.SSH_USER }}"

      - name: Salvar chave SSH corretamente
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' | awk '{print}' ORS='\n' > private_key
          echo "" >> private_key  # força newline no final
          chmod 600 private_key

      - name: Verificar permissão e tipo da chave
        run: |
          ls -l private_key
          file private_key

      - name: Testar conexão SSH (verbose)
        run: |
          ssh -vvv -i private_key -o StrictHostKeyChecking=no "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" "echo '✅ Conexão SSH bem-sucedida!'"
